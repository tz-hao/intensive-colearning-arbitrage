# 2026-08-10 — 模块 3 第 4 天：信号工作流验证（第一份信号日报）

> 共学第 6 天 | 任务：用 Hermes cron 定时跑价差检测，输出「值得看的信号」日报，校准误报率
> 产出：`scripts/signal_scanner.py`（scan() 可复用函数 + 3 轮真实检测）+ 第一份信号日报

---

## 一、任务与设计

### 目标

把 08-09 的 `lifi_routes.py`（一次性脚本）改造成**可被 cron 调用的信号扫描器**：
定时对多个场景调 LI.FI `/quote`，用修正后的闸门判断「这个时刻值不值得看一眼」，输出日报。

### 架构：signal_scanner.py

```
scan(scenario)          → 单场景 /quote → 净收益/成本/时长/是否触发   ← 环节③ 的复用函数
run_rounds(N, interval) → 定时跑 N 轮，带时间戳记录每轮结果
fmt_report()            → 生成 Markdown 信号日报（cron 可直接推送 / 落盘）
```

- **复用而非重写**：直接 `from lifi_routes import api_get, parse_route`，解析逻辑 08-09 已验证
- **场景**：USDC 10K Arb→Eth、USDC 10K Arb→Base（跨链稳定币搬运，模块 1 七种结构之一）
- **闸门**：`净收益 >= $10` 才触发 🚨 —— 净收益 = toAmount - fromAmount，LI.FI 已扣全部费用（含 $25 固定费），所以这个闸门意味着「真实赚到 $10+」

### 为什么闸门是「净收益 ≥ $10」而不是旧版「净收益 > $10」？

08-08 流水线设计时不知道 LI.FI 固定费，以为净收益 > $10 很容易达到。
08-09 实测后修正认知：**固定费 $25 已经在 toAmount 里扣掉了**，净收益就是真实利润。
所以新闸门数字没变，但含义变了——它只在「价差 > 固定费 $25 + $10」时才可能触发。

---

## 二、真实运行结果（08-10 20:05-20:06 CST，3 轮 × 2 场景 = 6 次检测）

| 时间 | 轮 | 场景 | 净收益 | 时长 | 工具 | 触发? |
|------|----|------|--------|------|------|-------|
| 20:05:03 | 1 | USDC 10K Arb→Eth | -25.00 USD (-0.25%) | 1080s | polymerStandard | — |
| 20:05:03 | 1 | USDC 10K Arb→Base | -25.00 USD (-0.25%) | 1080s | polymerStandard | — |
| 20:05:38 | 2 | USDC 10K Arb→Eth | -25.00 USD (-0.25%) | 1080s | polymerStandard | — |
| 20:05:38 | 2 | USDC 10K Arb→Base | -25.00 USD (-0.25%) | 1080s | polymerStandard | — |
| 20:06:14 | 3 | USDC 10K Arb→Eth | -25.00 USD (-0.25%) | 1080s | polymerStandard | — |
| 20:06:14 | 3 | USDC 10K Arb→Base | -25.00 USD (-0.25%) | 1080s | polymerStandard | — |

**汇总：检测 6 次，触发 0 次（0/6），误报 0 次**

---

## 三、误报率校准结论

1. **无信号是当前的真实状态**：两个场景 3 轮全部 -$25.00（LIFI 固定费），与 08-09 的 17 条路由全亏一致。稳定币跨链在「无价差」时刻就是稳定的 -0.25%
2. **旧闸门 = 永不触发**：08-08 的「净收益 > $10」在跨链场景下不可能满足——固定费先把利润吃光。这不是闸门设错，是**场景本身在当前市场无利可图**
3. **触发率 0/6 本身就是校准结果**：信号系统正确地**保持了沉默**——比乱报警强
4. **误报率 0/0 的诚实解读**：无触发时「误报率」没有定义。真正的执行层误报（滑点/交易失败/被夹）只有到 08-18 Paper Trading 才有数据
5. **单日样本的局限**：20 分钟内稳定币跨链报价几乎不变。真正的误报率校准需要**多天多时点**数据——这正是 cron 定时跑的意义（08-19 正式挂监控告警）

---

## 四、踩坑记录

| 坑 | 现象 | 解法 |
|---|---|---|
| quote 响应无 steps | 日报工具列显示空白 | quote 单条响应的桥名在**顶层 `tool`** 字段，不在 steps 里 → `if not tools: tools = quote["tool"]` |
| 静态检查 import 报错 | Pyright 报 lifi_routes 无法解析 | 运行时 `sys.path.insert` 是动态的，静态分析看不到——不影响运行，跑通为准 |

---

## 五、与流水线的衔接（环节③ 已可复用）

```
08-08 流水线：价格源采集 → 价差计算 → LI.FI 报价 → 净收益判断 → 告警
                              ↑ 环节③ 今天升级为 scan() 函数，可被 cron 调用
今天验证：  LI.FI 报价 + 闸门判断 = 环节③④ 的最小闭环，跑出了第一份日报
08-19 待办： 挂 Hermes cron 定时执行 + 异常推送（告警环节），即完整流水线
```

**给 08-11 策略假设的输入**：
- 跨链稳定币搬运**必须有真实价差才值得扫描**——没有价差时信号系统 100% 沉默
- 固定费 $25 意味着：要么单笔 ≥ $8.3K（0.3% 目标），要么换**无 LI.FI 固定费的路径**（如直连桥），要么放弃跨链改做**同链 DEX 价差**（无桥费，只有 swap fee + gas）
- 08-11 从七种结构选 2-3 个时，**同链三角套 / DEX 价差**比跨链搬运对固定费更友好——这是今天日报给模块 4 的最重要输入

---

## 六、关键收获

1. **信号系统的第一职责是沉默**：6 次检测 0 触发，恰恰证明系统在正确地过滤噪音
2. **误报率校准 = 认知校准**：真正的校准不是调阈值数字，而是认识到「净收益已含固定费」这一事实——08-08 的流水线设计因此修正
3. **可复用 > 一次性**：08-09 的脚本今天直接 import 复用，10 分钟跑完验证——积累可复用代码比每天重写快得多
4. **下一步**：08-11 策略假设池——把「跨链 vs 同链」的固定费约束带进去选型

---

## 附：运行方式

```bash
python3 scripts/signal_scanner.py                        # 1 轮
python3 scripts/signal_scanner.py --rounds 3 --interval 30   # 3 轮定时
python3 scripts/signal_scanner.py --json                 # JSON 输出（cron 消费）
```
